[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Ensure all routes are handled by the SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true